.page-header
  %h1
    -if @emoji.new_record?
      New Emoji
    -else
      Editing #{@emoji.name}

=form_for [:admin,@emoji], html:{class:"form-horizontal"} do |f|
  =f.bs_text_field :name, label_name: "Name / Pattern"
  =f.bs_file_field :image
  =f.bs_submit
